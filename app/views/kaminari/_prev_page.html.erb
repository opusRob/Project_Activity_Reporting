<%# Link to the "Previous" page
  - available local variables
    url:           url to the previous page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<!-- <span class="prev"> -->
<%
=begin
%>
	<%= link_to_unless current_page.first?, t('views.pagination.previous').html_safe, url, :rel => 'prev', :remote => remote %>
	<%= link_to_bootstrap_glyphicon_unless current_page.first?, url, "sm", "default", t('views.pagination.prev').html_safe, ["step-backward"], {remote: remote} %>
<%
=end
%>
	<% button_options = {} %>
	<%if current_page.first? %>
		<% button_options[:class] = "btn-disabled" %>
		<% button_options[:disabled] = true %>
	<% else %>
		<% button_options[:onclick] = "location.href='#{url}'; " %>
	<% end %>
	<%= button_tag_bootstrap_glyphicon \
		"button" \
		, "btn_pagination_prev_page" \
		, "sm" \
		, "default" \
		, t('views.pagination.previous').html_safe \
		, ["step-backward"] \
		, options = button_options \
	%>
<!-- </span> -->
